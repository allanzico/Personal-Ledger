{% extends 'body_base.html.twig' %}

{% block content_body %}

<h1>New Debit</h1>

{{ form_start(debitForm) }}
{{ form_widget(debitForm) }}

<button type="submit" class="btn btn-primary">Add</button>
{{ form_end(debitForm )}}

{% endblock %}